"""netlens-mcp: an MCP server for unobstructed web reading.

Fetches any URL directly with browser-like headers (past naive bot filters) and
returns the FULL page as clean Markdown — not a summary, no third-party reader.
Pure Python standard library, zero dependencies.
"""

__version__ = "0.1.2"
